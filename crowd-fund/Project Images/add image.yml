info:
  name: add image
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseUrl}}/projects/:id/images/"
  params:
    - name: id
      value: "22"
      type: path
  body:
    type: multipart-form
    data:
      - name: image
        type: text
        value: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
