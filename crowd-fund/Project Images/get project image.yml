info:
  name: get project image
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/projects/:id/images"
  params:
    - name: id
      value: "22"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
