info:
  name: Create Categories
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseUrl}}/projects/categories/"
  body:
    type: json
    data: |-
      {
        "name":"Sports"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
