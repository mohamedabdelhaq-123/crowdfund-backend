info:
  name: Delete Comment
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{baseUrl}}/projects/comments/1/"
  body:
    type: json
    data: |-
      {
      "content": "Edited comment text"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
