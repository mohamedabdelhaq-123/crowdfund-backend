info:
  name: Create Comment
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseUrl}}/projects/8/comments/"
  body:
    type: json
    data: |-
      {
      "content": "This project looks good"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
