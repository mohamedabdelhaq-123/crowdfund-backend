info:
  name: Update Comment
  type: http
  seq: 2

http:
  method: PATCH
  url: "{{baseUrl}}/projects/comments/2/"
  body:
    type: json
    data: |-
      {
      "content": "Edited comment text"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
