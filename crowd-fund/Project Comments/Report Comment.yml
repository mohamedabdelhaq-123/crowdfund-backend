info:
  name: Report Comment
  type: http
  seq: 5

http:
  method: POST
  url: "{{baseUrl}}/projects/comments/2/report/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
