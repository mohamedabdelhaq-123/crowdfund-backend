info:
  name: Get Comments
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/projects/8/comments/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
