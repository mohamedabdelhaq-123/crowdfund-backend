info:
  name: Get Comments
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/projects/2/comments/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
