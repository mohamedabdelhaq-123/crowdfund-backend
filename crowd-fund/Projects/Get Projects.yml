info:
  name: Get Projects
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/projects/"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
