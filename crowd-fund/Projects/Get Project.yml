info:
  name: Get Project
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/projects/:id/"
  params:
    - name: id
      value: "36"
      type: path

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
