info:
  name: Rate Project
  type: http
  seq: 8

http:
  method: POST
  url: "{{baseUrl}}/projects/8/rate/"
  body:
    type: json
    data: |-
      {
      "stars": 3
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
