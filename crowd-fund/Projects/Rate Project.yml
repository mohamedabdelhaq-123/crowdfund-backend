info:
  name: Rate Project
  type: http
  seq: 8

http:
  method: POST
  url: "{{baseUrl}}/projects/:id/rate/"
  params:
    - name: id
      value: "22"
      type: path
  body:
    type: json
    data: |-
      {
      "stars": 3
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
