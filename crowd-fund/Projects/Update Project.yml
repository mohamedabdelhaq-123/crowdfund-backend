info:
  name: Update Project
  type: http
  seq: 4

http:
  method: PATCH
  url: "{{baseUrl}}/projects/:id/"
  params:
    - name: id
      value: "1"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
