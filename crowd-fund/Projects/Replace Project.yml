info:
  name: Replace Project
  type: http
  seq: 3

http:
  method: PUT
  url: "{{baseUrl}}/projects/:id/"
  params:
    - name: id
      value: "1"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
