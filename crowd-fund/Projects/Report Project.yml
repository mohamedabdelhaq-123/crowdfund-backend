info:
  name: Report Project
  type: http
  seq: 7

http:
  method: POST
  url: "{{baseUrl}}/projects/8/report/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
