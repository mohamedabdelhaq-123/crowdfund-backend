info:
  name: Delete Project
  type: http
  seq: 6

http:
  method: DELETE
  url: "{{baseUrl}}/projects/:id/"
  params:
    - name: id
      value: "38"
      type: path

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
