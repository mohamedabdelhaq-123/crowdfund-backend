info:
  name: Replace Tag
  type: http
  seq: 3

http:
  method: PUT
  url: "{{baseUrl}}/tags/:id/"
  params:
    - name: id
      value: "1"
      type: path
  body:
    type: json
    data: |-
      {
        "name":"anime"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
