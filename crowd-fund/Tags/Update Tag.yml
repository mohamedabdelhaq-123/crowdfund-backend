info:
  name: Update Tag
  type: http
  seq: 4

http:
  method: PATCH
  url: "{{baseUrl}}/tags/:id/"
  params:
    - name: id
      value: "1"
      type: path
  body:
    type: json
    data: |-
      {
        "name":"movie",
        
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
