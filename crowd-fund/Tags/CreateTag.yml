info:
  name: CreateTag
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseUrl}}/tags"
  body:
    type: json
    data: |-
      {
        "name":"#MO"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
