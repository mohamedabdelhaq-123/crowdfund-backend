info:
  name: Delete Tag
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{baseUrl}}/tags/:id/"
  params:
    - name: id
      value: "1"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
