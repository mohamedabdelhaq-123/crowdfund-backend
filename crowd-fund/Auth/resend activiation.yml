info:
  name: resend activiation
  type: http
  seq: 5

http:
  method: POST
  url: "{{baseUrl}}/auth/resend-activation/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
