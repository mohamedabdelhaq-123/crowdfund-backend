info:
  name: resend activiation
  type: http
  seq: 5

http:
  method: POST
  url: "{{baseUrl}}/auth/resend-activation/"
  body:
    type: json
    data: |-
      {
          "email": "andrew14emad@gmail.com"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
