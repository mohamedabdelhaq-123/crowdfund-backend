info:
  name: login
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseUrl}}/auth/login/"
  body:
    type: json
    data: |-
      {
        "email": "mohamed.abdelhaqgp@gmail.com",
        "password": "SecurePass123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
