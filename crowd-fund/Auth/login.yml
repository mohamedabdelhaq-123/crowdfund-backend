info:
  name: login
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseUrl}}/auth/login/"
  body:
    type: json
    data: |-
      {
        "email": "tandrew14emad14@gmail.com",
        "password": "12345678"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
