info:
  name: Update Donation
  type: http
  seq: 5

http:
  method: PATCH
  url: "{{baseUrl}}/donations/:id/"
  params:
    - name: id
      value: "2"
      type: path
  body:
    type: json
    data: |-
      {
        "amount":500,
        "project":8,
        "user":1
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
