info:
  name: Get Donation By ID
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/donations/:id/"
  params:
    - name: id
      value: "39"
      type: path
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
