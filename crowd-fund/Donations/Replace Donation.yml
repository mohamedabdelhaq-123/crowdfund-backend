info:
  name: Replace Donation
  type: http
  seq: 4

http:
  method: PUT
  url: "{{baseUrl}}/donations/:id/"
  params:
    - name: id
      value: "2"
      type: path
  body:
    type: json
    data: |-
      {
        "amount":400,
        "project":8,
        "user":1
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
