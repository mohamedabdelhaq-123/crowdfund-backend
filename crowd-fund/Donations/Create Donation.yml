info:
  name: Create Donation
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseUrl}}/donations/:id/"
  params:
    - name: id
      value: "39"
      type: path
  body:
    type: json
    data: |-
      {
        "amount":1000000
       
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
