info:
  name: Delete Donation
  type: http
  seq: 6

http:
  method: DELETE
  url: "{{baseUrl}}/donations/:id/"
  params:
    - name: id
      value: "2"
      type: path
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
