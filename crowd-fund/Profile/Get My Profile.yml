info:
  name: Get My Profile
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/users/me/"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
