info:
  name: Get My Donations
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseUrl}}/users/me/donations/"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
